#!/bin/bash
# zed-launcher - Launch Zed Editor

# Check if installed
if ! command -v zed &> /dev/null; then
    echo "Zed not installed. Run the ide-chooser script to install."
    exit 1
fi

# Launch with arguments
exec zed "$@"