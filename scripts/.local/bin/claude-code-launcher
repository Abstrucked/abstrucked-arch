#!/bin/bash
# claude-code-launcher - Launch Claude Code

# Check if installed
if ! command -v claude &> /dev/null; then
    echo "Claude Code not installed. Run the ide-chooser script to install."
    exit 1
fi

# Launch with arguments
exec claude "$@"