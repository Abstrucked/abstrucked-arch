#!/bin/bash
# opencode-launcher - Launch OpenCode

# Check if installed
if ! command -v opencode &> /dev/null; then
    echo "OpenCode not installed. Run the ide-chooser script to install."
    exit 1
fi

# Launch with arguments
exec opencode "$@"