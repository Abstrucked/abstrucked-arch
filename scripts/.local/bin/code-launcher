#!/bin/bash
# code-launcher - Launch VS Code

# Check if installed
if ! command -v code &> /dev/null; then
    echo "VS Code not installed. Run the ide-chooser script to install."
    exit 1
fi

# Launch with arguments
exec code "$@"