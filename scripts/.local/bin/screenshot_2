#!/bin/sh
# Screenshot for secondary monitor (1920x1080)

timestamp="$(date +%Y%m%d%H%M%S)"
targetbase="$HOME/screenshots"
mkdir -p "$targetbase"
[ -d "$targetbase" ] || exit 1
scrot -a "3440,0,1920,1080" "$targetbase/$timestamp.png"
